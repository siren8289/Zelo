# AI: Groq 또는 Gemini 중 하나 이상 설정 (Groq 있으면 Groq 우선)
# Groq (서버에서만 사용, 키 노출 금지)
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant

# Gemini (서버에서만 사용, 키 노출 금지)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Supabase (클라이언트용)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase 서버 전용 (절대 NEXT_PUBLIC 붙이면 안 됨)
SUPABASE_SERVICE_ROLE_KEY=...
